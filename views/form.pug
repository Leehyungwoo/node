doctype html
html
    head
        meta(charset="UTF-8")
    body
        form(action="/form_receiver" method="post")
            p
                input(type="text" name="title")
            p
            p
                input(type="password" name="description")
            p
            input(type="submit")